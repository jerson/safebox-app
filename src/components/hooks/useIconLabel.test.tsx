import useIconTintLabel from './useIconTintLabel';

test('facebook', () => {
  expect(useIconTintLabel('facebook')).toBeTruthy();
});
test('github', () => {
  expect(useIconTintLabel('github')).toBeTruthy();
});
test('aperture', () => {
  expect(useIconTintLabel('aperture')).toBeTruthy();
});
test('box', () => {
  expect(useIconTintLabel('box')).toBeTruthy();
});
test('calendar', () => {
  expect(useIconTintLabel('calendar')).toBeTruthy();
});
test('photo', () => {
  expect(useIconTintLabel('photo')).toBeTruthy();
});
test('codepen', () => {
  expect(useIconTintLabel('codepen')).toBeTruthy();
});
test('term', () => {
  expect(useIconTintLabel('term')).toBeTruthy();
});
test('twitter', () => {
  expect(useIconTintLabel('twitter')).toBeTruthy();
});

test('database', () => {
  expect(useIconTintLabel('database')).toBeTruthy();
});

test('sql', () => {
  expect(useIconTintLabel('sql')).toBeTruthy();
});

test('gitlab', () => {
  expect(useIconTintLabel('gitlab')).toBeTruthy();
});

test('instagram', () => {
  expect(useIconTintLabel('instagram')).toBeTruthy();
});

test('chrome', () => {
  expect(useIconTintLabel('chrome')).toBeTruthy();
});

test('linkedin', () => {
  expect(useIconTintLabel('linkedin')).toBeTruthy();
});

test('slack', () => {
  expect(useIconTintLabel('slack')).toBeTruthy();
});

test('email', () => {
  expect(useIconTintLabel('email')).toBeTruthy();
});

test('gmail', () => {
  expect(useIconTintLabel('gmail')).toBeTruthy();
});

test('outlook', () => {
  expect(useIconTintLabel('outlook')).toBeTruthy();
});
